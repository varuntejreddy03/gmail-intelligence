import { ComposeModal } from "@/components/compose/ComposeModal";

/** Renders the AI-assisted full-page email composer. */
export default function ComposePage(): JSX.Element {
  return <ComposeModal />;
}
